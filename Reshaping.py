import numpy as np

# Define the original array with 6 elements
arr = np.array([1, 2, 3, 4, 5, 6])

# Reshape the array to a shape of (3, 2)
reshaped = arr.reshape((3, 2))

# Print the reshaped array
print(reshaped)
